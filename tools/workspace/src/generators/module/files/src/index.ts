/**
 * Module Entry Point
 *
 * Exports the server component Module for use in the module registry.
 * Module.tsx is a server component that can call server-only code and
 * renders ClientModule.tsx for interactive UI.
 */
export { Module } from "./lib/module";

